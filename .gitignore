# Dependencies
node_modules/

# Wrangler
.wrangler/
.wrangler-deploy/
.wrangler-state/
.dev.vars
.miniflare/

# Local secrets / env (never commit real secrets — use `wrangler secret put`)
.dev.vars.*
.env
.env.*
*.local

# Build output
dist/
.out/

# Deploy-bot fetches these prebuilt artifacts from GitHub raw; keep them tracked:
!dist/nexus-panel.mjs
!dist/assets-manifest.json
!dist/dashboard-files.json

# OS / editor
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/

# Optional: keep wrangler.toml tracked (it contains only non-secret [vars] + placeholders).
# If you ever paste real IDs/tokens into it, uncomment the line below.
# wrangler.toml
